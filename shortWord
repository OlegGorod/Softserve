Напишіть функцію, що отримує у якості аргумента рядок слів та повертає довижину найкоротшого.
Example: "bitcoin take over the world maybe who knows perhaps" --> 3

function getShortWord(phrase) {
let phraseArr = phrase.split(' ');
let result = [];
for (let i = 0; i < phraseArr.length; i++) {
        result[i] = phraseArr[i].length;
   };
return Math.min(...result);
};

getShortWord("Lets all go on holiday somewhere very cold");

--- corporate Answer

const getShortW = phrase => Math.min(...phrase.split(' ').map(ph => ph.length));
console.log(getShortW("Lets all go on holiday somewhere very cold"));
